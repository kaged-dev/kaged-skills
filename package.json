{
  "name": "@kaged/skills",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Public registry of kaged skills. Publishes skills.kaged.dev.",
  "homepage": "https://skills.kaged.dev",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "validate": "bun run scripts/validate.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/node": "^22.13.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "bun": ">=1.3.9"
  }
}
